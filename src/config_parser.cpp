#include "config_parser.hpp"

ConfigParser::ConfigParser(const std::string& path) : path_(path) {}

std::map<std::string, ProgramConfig> ConfigParser::parse() {
    // Parse YAML file and return map of program configs
    return {};
}
